#!/usr/bin/env tsx
/**
 * One-shot fix: add *En fields to admin API (POST + PATCH) + PRODUCT_FIELDS constant.
 * Run: npx tsx scripts/_admin-en-fix.ts  (no-op verification; actual fix is via Edits below)
 */
console.log("See Edits in chat — this script is a no-op placeholder.");
