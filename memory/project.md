> Scan stamp: 2026-07-02T04:34:42.232Z
> package.json mtime: 2026-06-30T00:22:26.729Z
> schema.prisma mtime: 2026-07-02T02:12:18.899Z

# Project

## Stack

Next.js 16.2.9 · React 19.2.4 · Prisma ^6.19.3 · Tailwind 4 · Framer Motion 12 · TypeScript ^5

Key deps: `@tiptap/extension-color`, `@tiptap/extension-highlight`, `@tiptap/extension-image`, `@tiptap/extension-link`, `@tiptap/extension-placeholder`, `@tiptap/extension-text-align`, `@tiptap/extension-text-style`, `@tiptap/extension-underline`, `@tiptap/pm`, `@tiptap/react`, `@tiptap/starter-kit`, `bcryptjs` (+10 more).

### Run scripts

- `dev`
- `build`
- `start`
- `lint`
- `scan`

## Provider tree (root layout)

`LangProvider > AuthProvider > AppProvider > ToastProvider`. See `src/app/layout.tsx`.

- LangContext — URL-based locale (`/en/*` → en, else fa), sets `lang` cookie, syncs `<html lang|dir>`. `src/context/LangContext.tsx`
- AuthProvider — receives `initialUser` from server, calls `refetch()` on mount to verify session. `src/context/AuthContext.tsx`
- AppContext — legacy client state (cart/licenses/tickets/toasts/session). `src/context/AppContext.tsx`

## Middleware matcher

```
matcher: [ ... ]
/*
       * Match all paths except:
       * - api routes
       * - static files (images, fonts, css, js)
       * - _next internals
       */
      "/((?!api|_next/static|_next/image|favicon.ico|images|uploads).*)",
    ]
```

## Layout duties (`src/app/layout.tsx`)

- resolves initial user from `gc_session` httpOnly cookie via `getInitialUser()`
- derives `lang` from URL, emits hreflang/canonical via `x-url` header

## Lib surface (`src/lib`)

`lib/auth-utils.ts`, `lib/currency.ts`, `lib/html-sanitizer.ts`, `lib/license-crypto.ts`, `lib/middleware.ts`, `lib/orderNumber.ts`, `lib/prisma.ts`, `lib/rate-limiter.ts`, `lib/security-headers.ts`.

Sub-modules: `lib/admin/`, `lib/api/`, `lib/license/`, `lib/pricing/`, `lib/repositories/`, `lib/seo/`, `lib/services/`.

## Env vars (names only — live values in `.env`)

`KAVENEGAR_API_KEY`, `KAVENEGAR_TEMPLATE`, `LICENSE_ENCRYPTION_KEY`, `NODE_ENV`, `OPENROUTER_API_KEY`.

## Local scratch (NOT committed / regenerated)

- `graphify-out/` — AST cache of the graphify skill; regenerated on each skill run (.gitignored)

---

_Regenerated by `scripts/scan-project.ts`. Do not edit by hand._
